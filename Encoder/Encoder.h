// guardar no arquivo compactado:

// Quantos caracteres existem na tabela
// Qual o tamanho de cada frequência (pode ser que haja frequências maiores e menores)
// Tabela de caracteres-frequência
// Quantos bits da árvore existem
// Só então os bits da árvore (0 para esc, 1 para dir)
