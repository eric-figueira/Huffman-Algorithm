#include "ByteFrequency.h"

class Frequency 
{
    ByteFrequency vet[256];

    // incrementar o valor se não for nulo, s

    // Quantos bits devem ser desprezados no último byte
    // Quantos caracteres tem
    // Tabela de caracteres frequência
    // Só então os bits da árvore (0 para esc, 1 para dir)
};